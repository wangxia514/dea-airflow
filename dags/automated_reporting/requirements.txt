# These are needed by the venv to run automated dags
# They should be installed in a venv using python 3.6
# Python and package versions should be alligned with Airflow

psycopg2-binary==2.9.1
requests==2.26.0
pytz==2021.1
python-dateutil==2.8.2
